node_modules/
# Your real config (KV id, domain) — copy from wrangler.jsonc.example. Gitignored so
# nothing real lands in the public repo.
wrangler.jsonc
# Local secret (TOKEN_SALT) for `wrangler dev` and `npm run token`.
.dev.vars
.wrangler/
dist/
*.log
.DS_Store
