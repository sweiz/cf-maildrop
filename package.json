{
  "name": "cf-maildrop",
  "version": "1.0.0",
  "description": "Disposable dev email inbox on Cloudflare: one Email Routing rule + subaddressing -> KV (auto-expiring) -> token-protected REST API + web GUI. Deploys locally via `wrangler login`. Stays inside the free tier.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "token": "node scripts/token.mjs"
  },
  "dependencies": {
    "postal-mime": "^2.4.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260601.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.100.0"
  },
  "allowScripts": {
    "esbuild@0.27.3": true
  }
}
